apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: attestor-api
spec:
  secretName: attestor-tls
  dnsNames:
    - attestor.example.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
