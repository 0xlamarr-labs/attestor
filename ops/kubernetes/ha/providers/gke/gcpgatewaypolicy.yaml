apiVersion: networking.gke.io/v1
kind: GCPGatewayPolicy
metadata:
  name: attestor
spec:
  default:
    sslPolicy: attestor-modern-tls
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: attestor
